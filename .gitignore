.env

# Python bytecode / caches
__pycache__/
*.py[cod]
*$py.class

# Local SQLite DB / instance state (not for repo)
instance/proclean.db
instance/proclean.db-journal
instance/proclean.db-wal
instance/proclean.db-shm

# Headcount SUA real PDFs (private local fixtures only)
private_fixtures/

# INFONAVIT sample PDFs (real data, never commit)
AVI_*.PDF
AVI_*.pdf
*infonavit*real*.pdf
*INFONAVIT*REAL*.PDF

# Comparación visual PDF (datos reales, no versionar)
tmp/pdf_regression/

# Banorte operative / private samples (real PII — never commit)
private_fixtures/banorte/
NOMINA BANORTE.xlsx
Reporte_Detallado_*.xlsx
NI67059*.pag

